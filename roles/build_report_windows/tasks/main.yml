- name: create HTML report if jinja include works
  ansible.builtin.template:
    src: report.j2
    dest: "{{ file_path }}/windows.html"
    owner: root
    group: root
    mode: '0644'
  when: usesingletemplate == false

- name: create HTML report if jinja include doesn't work
  ansible.builtin.template:
    src: fullreport.j2
    dest: "{{ file_path }}/windows.html"
    owner: root
    group: root
    mode: '0644'
  when: usesingletemplate == true

- name: copy CSS over
  ansible.builtin.copy:
    src: "css"
    dest: "{{ file_path }}"
    directory_mode: true
    owner: root
    group: root
    mode: '0644'

- name: copy logos over
  ansible.builtin.copy:
    src: "{{ item }}" 
    dest: "{{ file_path }}"
    directory_mode: true
    owner: root
    group: root
    mode: '0644'
  loop:
    - "webpage_logo.png"
    - "redhat-ansible-logo.svg"
    - "server.png"

- name: display link to inventory report
  ansible.builtin.debug:
    msg: "Please go to http://{{ ansible_host }}/windows.html"
